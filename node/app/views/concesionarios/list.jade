article.article
  h4
    a(href='/concesionarios/'+concesionario._id, title=concesionario.nombre) #{concesionario.nombre}

  //- .meta
  //-   span.muted= formatDate(concesionario.createdAt)

  //-   - if (article.user)
  //-     | &nbsp; - &nbsp;
  //-     span Author &nbsp;

  //-     - var name = article.user.name ? article.user.name : article.user.username

  //-     a(href="/users/"+article.user._id)= name

  //-   - if (article.tags)
  //-     | &nbsp; - &nbsp;
  //-     span Tags &nbsp;
  //-     - each tag in article.tags.split(',')
  //-       i.muted.fa.fa-tag &nbsp;
  //-       a.tag(href="/tags/"+tag)= tag
  //-       | &nbsp;&nbsp;
